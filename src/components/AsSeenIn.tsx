export default function AsSeenIn() {
  return (
    <div className="mt-14 w-full border-t border-border pt-8">
      <div className="flex flex-col gap-5 sm:flex-row sm:items-center sm:gap-8">
        <p className="shrink-0 text-xs font-mono uppercase tracking-[0.25em] text-muted">
          As seen in
        </p>

        <p className="text-sm font-semibold tracking-tight text-faint">
          Nowhere Yet..
        </p>
      </div>
    </div>
  );
}
